# Deployment Guide

- Build backend and dashboard images.
- Use managed PostgreSQL with PostGIS and managed Redis.
- Set real API keys and auth providers in environment variables.
- Add observability (Sentry + metrics + logs).
