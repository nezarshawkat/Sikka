# Setup Instructions

1. Install Docker, Node.js, Flutter SDK.
2. Copy `.env.example` files.
3. Start compose stack from `docker/docker-compose.yml`.
4. Apply SQL migrations and seed files.
5. Run mobile app with `flutter run`.
